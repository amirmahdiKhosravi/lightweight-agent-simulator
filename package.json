{
  "name": "lightweight-agent-simulator",
  "private": true,
  "description": "A full-stack web app that simulates AI agent mechanics — intent parsing, tool routing, and execution tracing — without LLMs or external APIs. Built with FastAPI and React/Vite, it demonstrates the core patterns behind LangChain and Semantic Kernel in a transparent, dependency-free way.",
  "scripts": {
    "install": "npm run install:frontend && npm run install:backend",
    "install:frontend": "cd frontend && npm install",
    "install:backend": "node scripts/install-backend.js",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "node scripts/start-backend.js",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
